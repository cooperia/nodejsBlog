<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
		<% for(i=0;i<data.length; i++){ %>
			<article>
				<dt><a href="/blog/<%= data[i]._id %>"><%= data[i].title %></a></dt>
				
				<dd><%= data[i].body %></dd>
				<dd><%= data[i].created_at %></dd>

			<% for(j=0; j<data[i].comments.length; j++){%>
				<dd>
					<article>
						<dt>Author: <%= data[i].comments[j].author %></dt>
						<dd>Comment: <%= data[i].comments[j].comment %></dd>
					</article>
				</dd>
			</article>
		<%} %>
		<%} %>
  </body>
</html>